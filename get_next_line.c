#include "get_next_line.h"

char *ft_get_line_from_reminder(char *reminder)
{
	char *line;
	if (!reminder)
		return (NULL);
	line = ft_strchr(reminder, '\n');

	return ();
}
char	*get_next_line(int fd)
{
	
}
